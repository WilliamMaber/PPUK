import React from "react";
import ReactDOM from "react-dom";
import EventList from "./EventList.jsx";
const root = ReactDOM.render(
  React.createElement(EventList),
  document.getElementById("EventListContainer")
);
