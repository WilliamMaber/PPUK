import React from "react";
import ReactDOM from "react-dom";
import PasswordChange from "./PasswordChange.jsx";
ReactDOM.render(
  React.createElement(PasswordChange),
  document.getElementById("PasswordChangeContainer")
);
