import React from 'react';
import ReactDOM from 'react-dom';
import UserStatus from './userStatus.jsx';
ReactDOM.render(React.createElement(UserStatus), document.getElementById('userStatusContainer'));